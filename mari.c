#include <stdio.h>
#include <stdlib.h>
int blocoPilar(){
    // float alturapilar, largurapilar, comprimentopilar, volumepilar, masseiras, cimento, areia, agua, areiaM3, seixo, seixoM3;
    // printf ("Informe a altura do pilar em metros: \n\n");
    // scanf ("%f", &alturapilar);
    // printf ("Informe a largura do pilar em metros: \n\n");
    // scanf ("%f", &largurapilar);
    // printf ("Informe o comprimento do pilar em metros: \n\n");
    // scanf ("%f", &comprimentopilar);
    //
    // volumepilar = (alturapilar)*(largurapilar)*(comprimentopilar)*1.4;
    // printf ("Volume do pilar: %.4f \n\n", volumepilar);
    // masseiras = (volumepilar*1000)/(6.0*18);
    // cimento = masseiras;
    // areia = 2.0*masseiras;
    // seixo = 3.0*masseiras;
    // agua = 0.5*masseiras;
    //
    // printf ("O numero de masseiras necessarias para concretar um pilar e: %.0f \n\n", masseiras);
    // printf ("O numero de sacas de cimento necessarias para um pilar eh: %.0f sacas.\n\nO numero de latas de areia necessarias eh: %.0f latas.\n\nO numero de latas de seixo eh: %.0f latas\n\nO numero de latas de agua eh: %.0f latas.\n\n", cimento*0.5, areia, seixo, agua);
    // areiaM3 = areia*(18*0.001);
    // seixoM3 = seixo*(18*0.001);
    // printf ("consumo de areia em m3: %.4f.\n\nconsumo de seixo em m3: %.4f.\n\n", areiaM3, seixoM3);
    //
    // int m,n;
    // float matrizQtd [1][3];
    //
    // matrizQtd [0][0] = 0.5*cimento;
    // matrizQtd [0][1] = areiaM3;
    // matrizQtd [0][2] = seixoM3;
    //
    // printf("Digite o valor unitario de cimento, areia e seixo respectivamente:\n\n");
    //
    // float matrizPreco [3][1];
    //
    // for (m=0;m<3;m++){
    //     for (n=0;n<1;n++){
    //     //printf("Informe o preco unitario:\n\n");
    //     scanf ("%f", &matrizPreco[m][n]);
    //     }
    // }
    //
    // float matrizCusto[3][3];
    //
    // for (m=0;m<3;m++){
    //     for (n=0;n<3;n++){
    //     matrizCusto[m][n] = matrizPreco[m][n]*matrizQtd[m][n];
    //     }
    // }
    //
    // float soma=0.0;
    //
    // for (m=0;m<1;m++){
    //     for (n=0;n<3;n++){
    //
    //     //printf ("O preco unitario de cada material eh: %.2f \n\n", matrizCusto[m][n]);
    //     soma=matrizCusto[0][0]+ matrizCusto[0][1] + matrizCusto[0][2];
    //     }
    //     printf ("O preco final eh: R$%.2f\n\n", soma);
    // }
    // int qtdpedreiros, qtdserventes, prazo;
    // float produtividade, indice, qtdequipes,jornada, dias, qtdmadeira;
    //
    // printf ("Quantos m3 de concreto voce adensa em uma hora ? \n\n");
    // scanf ("%f", &produtividade);
    // indice = 1/(produtividade);
    // printf ("Quantos pedreiros na equipe ? \n\n");
    // scanf("%d", &qtdpedreiros);
    // printf ("Quantos serventes ? \n\n");
    // scanf("%d", &qtdserventes);
    // printf ("Qual o prazo em dias para finalizar o serviço ? \n\n");
    // scanf ("%d", &prazo);
    // printf ("Informe a jornada diaria de trabalho: \n\n");
    // scanf("%d", &jornada);
    //
    // dias = (volumepilar*indice)/(jornada);
    // qtdequipes = (dias)/(prazo);
    // if (qtdequipes > 1){
    //     printf ("voce precisara de mais gente na equipe para cumprir o prazo.\n\n");
    //     /*printf ("Terminara a atividade em: %.1f \n\n", dias);*/
    //
    // }
    // else{
    //     printf ("sua equipe e suficiente e terminara a atividade em: %.1f \n\n", dias);
    // }
    // return volumepilar;

}

void blocoSapata(){
  // float alturaSapata,larguraSapata,comprimentoSapata,volumeSapata,numMasseiras,cimento,areia,agua,areiaM3,seixo,seixoM3;
  // printf ("Informe a altura da sapata em metros: \n\n");
  // scanf ("%f", &alturaSapata);
  // printf ("Informe a largura da sapata em metros: \n\n");
  // scanf ("%f", &larguraSapata);
  // printf ("Informa o comprimento da sapata em metros: \n\n");
  // scanf ("%f", &comprimentoSapata);
  //
  // volumeSapata = ((alturaSapata)*(larguraSapata)*(comprimentoSapata))*1.4;
  // printf("%f\n\n",volumeSapata);
  //
  // numMasseiras = (volumeSapata*1000)/(6.0*18);
  // printf("%f\n\n",numMasseiras);
  // cimento = numMasseiras;
  // areia = 2.0*numMasseiras;
  //
  // seixo = 3.0*numMasseiras;
  //
  // agua = 0.5*numMasseiras;
  //
  // printf ("O numero de masseiras necessarias para concretar uma sapata eh: %.0f masseiras.\n\n", numMasseiras);
  // printf ("O numero de sacas de cimento necessarias para uma sapata eh: %.0f sacas.\n\nO numero de latas de areia necessarias eh: %.0f latas.\n\nO numero de latas de seixo eh: %.0f latas\n\nO numero de latas de agua eh: %.0f latas.\n\n", cimento*0.5, areia, seixo, agua);
  // areiaM3 = areia*(18*0.001);
  // seixoM3 = seixo*(18*0.001);
  // printf ("consumo de areia em m3: %.4f.\n\nconsumo de seixo em m3: %.4f.\n\n", areiaM3, seixoM3);
  //
  // int m,n;
  // float matrizQtd [1][3];
  //
  // matrizQtd [0][0] = 0.5*cimento;
  // matrizQtd [0][1] = areiaM3;
  // matrizQtd [0][2] = seixoM3;
  //
  // printf("Digite o valor unitario de cimento, areia e seixo respectivamente:\n\n");
  //
  // float matrizPreco [3][1];
  //
  // for (m=0;m<3;m++){
  //   for (n=0;n<1;n++){
  //
  //     scanf ("%f", &matrizPreco[m][n]);
  //   }
  // }
  //
  // float matrizCusto[3][3];
  //
  // for (m=0;m<3;m++){
  //   for (n=0;n<3;n++){
  //     matrizCusto[m][n] = matrizPreco[m][n]*matrizQtd[m][n];
  //   }
  // }
  //
  // float soma=0.0;
  //
  // for (m=0;m<1;m++){
  //   for (n=0;n<3;n++){
  //
  //     soma=matrizCusto[0][0]+ matrizCusto[0][1] + matrizCusto[0][2];
  //   }
  //   printf ("O preco final eh: R$%.2f\n\n", soma);
  // }
  //   int qtdpedreiros, qtdserventes, prazo;
  //   float produtividade, indice, qtdequipes,jornada, dias, qtdmadeira;
  //
  //   printf ("Quantos m3 de concreto voce adensa em uma hora ? \n\n");
  //   scanf ("%f", &produtividade);
  //   indice = 1/(produtividade);
  //   printf ("Quantos pedreiros na equipe ? \n\n");
  //   scanf("%d", &qtdpedreiros);
  //   printf ("Quantos serventes ? \n\n");
  //   scanf("%d", &qtdserventes);
  //   printf ("Qual o prazo em dias para finalizar o serviço ? \n\n");
  //   scanf ("%d", &prazo);
  //   printf ("Informe a jornada diaria de trabalho: \n\n");
  //   scanf("%d", &jornada);
  //   int volumepilar = blocoPilar();
  //   dias = (volumepilar*indice)/(jornada);
  //   qtdequipes = (dias)/(prazo);
  //   if (qtdequipes > 1){
  //       printf ("voce precisara de mais gente na equipe para cumprir o prazo.\n\n");
  //       /*printf ("Terminara a atividade em: %.1f \n\n", dias);*/
  //
  //   }
  //   else{
  //       printf ("sua equipe e suficiente e terminara a atividade em: %.1f \n\n", dias);
  //   }

}

void blocoAlvenaria(){
    float Atijolo, Ltijolo, Ctijolo, juntaV, juntaH, Areaparede, Ntijolo, qtdtijolo, Volargamassa, voltotal, masseiras, cimento, areia, cal, areiam3;
    printf ("Informe a altura do tijolo em cm: \n\n");
    scanf ("%f", &Atijolo);
    printf("Atijolo: %f\n\n", Atijolo);
    printf ("Informe a largura do tijolo em cm: \n\n");
    scanf ("%f", &Ltijolo);
    printf("Ltijolo: %f\n\n", Ltijolo);
    printf ("Informe o comprimento do tijolo em cm: \n\n");
    scanf ("%f", &Ctijolo);
    printf("Ctijolo: %f\n\n", Ctijolo);
    printf ("informe a junta vertical do tijolo em cm: \n\n");
    scanf ("%f", &juntaV);
    printf("juntaV: %f\n\n", juntaV);
    printf("informe a junta horizontal do tijolo em cm: \n\n");
    scanf ("%f", &juntaH);
    printf("juntaH: %f\n\n", juntaH);
    printf ("informe a area da parede a ser construida: \n\n");
    scanf ("%f", &Areaparede);
    printf("Area da parede: %f\n\n", Areaparede);
    // Ntijolo = 10500/((Atijolo+juntaV)*(Ctijolo+juntaH));
    // qtdtijolo = Ntijolo*Areaparede;
    // Volargamassa = (1-(Ntijolo*Ctijolo*Atijolo)/10000)*(Ltijolo/100);
    // voltotal = Volargamassa*Areaparede*1000;
    // masseiras = voltotal/(9.5*18);
    // cimento = 0.5*masseiras;
    // cal = (0.5*masseiras)/0.93;
    // areia = 8*masseiras;
    // printf("O numero de masseiras e: %.0f \n\n", masseiras );
    // printf ("A quantidade de tijolos necessaria e: %.0f \n\n", qtdtijolo);
    // printf ("A quantidade de sacos de cimento e: %.0f sacas.\n\n A quantidade de sacas de cal e: %.0f cal. \n\n A quantidade de latas de areia e: %.0f. \n\n.", cimento, cal, areia);
    // areia = areia*(18*0.001);
    // printf ("%.4f \n\n", areia);
    //
    // int m,n;
    // float matrizQtd [1][4];
    // matrizQtd [0][0] = qtdtijolo;
    // matrizQtd [0][1] = cimento;
    // matrizQtd [0][2] = cal;
    // matrizQtd [0][3] = areia;
    // printf("Digite o valor unitario de tijolo. cimento, cal e areia respectivamente:\n\n");
    //
    // float matrizPreco [4][1];
    //
    // for (m=0; m<4; m++){
    //     for (n=0; n<1; n++){
    //         scanf ("%f", &matrizPreco[m][n]);
    //     }
    // }
    //
    // float matrizCusto [4][4];
    // for (m=0; m<4; m++){
    //     for (n=0; n<4; n++){
    //         matrizCusto[m][n] = matrizPreco[m][n]*matrizQtd[m][n];
    //     }
    // }
    //
    // float soma = 0.0;
    // for (m=0; m<1; m++){
    //     for (n=0; n<4; n++){
    //         soma = matrizCusto[0][0] + matrizCusto[0][1] + matrizCusto [0][2] + matrizCusto [0][3];
    //
    //     }
    //      printf ("O preco final eh: R$%.2f\n\n", soma);
    // }
    int qtdpedreiros, qtdserventes;
    float produtividade, indice, qtdequipes,jornada, dias, prazo;
    //
    printf ("Quantos metros de alvenaria vc,pedreiro,levanta em uma hora ? \n\n");
    scanf ("%f", &produtividade);
    indice = 1/(produtividade);
    printf("produtividade: %f\n\n", produtividade);
    printf("indice: %f\n\n", indice);
    printf ("Quantos pedreiros na equipe ? \n\n");
    scanf("%d", &qtdpedreiros);
    printf("quantidade de pedreiros: %d\n\n", qtdpedreiros);
    printf ("Quantos serventes ? \n\n");
    scanf("%d", &qtdserventes);
    printf("quantidade de serventes: %d\n\n", qtdserventes);
    printf ("Qual o prazo em dias para finalizar o serviço ? \n\n");
    scanf ("%f", &prazo);
    printf("prazo: %f\n\n", prazo);
    printf ("Informe a jornada diaria de trabalho: \n\n");
    scanf("%f", &jornada);
    printf("jornada: %f\n\n", jornada);
    //
    dias = (Areaparede*indice)/(jornada);
    //
    qtdequipes = (dias)/(prazo);
    system("clear");
    printf("Monitor: qtdequipes = %f\n\n", qtdequipes);
    printf("Monitor: dias = %f\n\n", dias);
    printf("Monitor: prazo = %f\n\n", prazo);
    if (qtdequipes > 1){
        printf ("voce precisara de mais uma equipe para cumprir o prazo.\n\n");
        printf ("Terminara a atividade em: %f \n\n", dias);

    }
    else{
        printf ("sua equipe e suficiente e terminara a atividade em: %f \n\n", dias);
    }

}

int main ()
{
    char nome [60];
    printf ("Ola, informe seu nome, por favor: \n\n");
    scanf ("%s", nome);
    printf ("Bem-vindo a calculadora e-civil %s", nome);
    int num;
    printf ("Nos Informe o que deseja quantificar\n\n");
    printf ("1 - concreto para sapata \n\n");
    printf ("2 - concreto para pisos \n\n");
    printf ("3 - alvenaria \n\n");
    printf ("4 - sair \n\n");
    scanf ("%d", &num);
    switch (num)
    {
    case 1:
        blocoSapata();
        break;
    case 2:
        blocoPilar();
        break;
    case 3:
        blocoAlvenaria();
        break;
    case 4:
        printf ("Obrigado, volte sempre %s \n\n", nome);
        break;
    default:
        printf ("Ok \n\n");
    }
    return 0;

}
